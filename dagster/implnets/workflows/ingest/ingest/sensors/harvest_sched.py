# this will need to read the tennants file, and perhaps the gleaner config
# and write out the schedule.
# other projects (oih) have the harvest details for a source in the gleaner config.
# that probably needs to be added to the model, so that we can retrieve it.
